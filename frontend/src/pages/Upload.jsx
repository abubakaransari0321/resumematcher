import ResumeMatcher from './ResumeMatcher';

// The Upload page now uses the modern ResumeMatcher component
// which provides the complete three-step matching experience
const Upload = () => {
  return <ResumeMatcher />;
};

export default Upload;
